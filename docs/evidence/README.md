# Python benchmark evidence

`python-static-dynamic-500.json` is the current formal Python
`scene-engine==0.6.1` server-side 632-static + 500-dynamic acceptance run.

`scene-engine-500.json` is historical `scene-engine==0.6.0` evidence retained
for the earlier 500-node architecture report; it is not 0.6.1 release
evidence. Renderer evidence under `render-runtime-v2/` belongs to its own
JavaScript release tuple.
