{
  "name": "scene-engine-workspace",
  "version": "0.9.0",
  "private": true,
  "type": "module",
  "packageManager": "npm@11.9.0",
  "engines": {
    "node": "^20.19.0 || >=22.12.0",
    "npm": ">=10 <12"
  },
  "workspaces": [
    "js/packages/*"
  ],
  "scripts": {
    "test": "npm test --workspaces --if-present && npm run typecheck",
    "verify": "uv run python scripts/verify_cutover.py",
    "typecheck": "tsc -p js/types/tsconfig.json"
  },
  "devDependencies": {
    "typescript": "5.8.3"
  }
}
